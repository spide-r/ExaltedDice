package me.spider.commands.sheets;

public class ModifyEssenceDropdown {

    //todo this might be helpful later
    /*
        event.reply("Select your essence").addActionRow(StringSelectMenu.create("choose-essence")
                .addOptions(SelectOption.of("personalMotes", "Personal Motes"),
                        SelectOption.of("personalMax", "Max Personal Motes"),
                        SelectOption.of("peripheralMotes", "Peripheral Motes"),
                        SelectOption.of("peripheralMax", "Max Peripheral Motes"),
                        SelectOption.of("otherMotes", "Other Motes"),
                        SelectOption.of("otherMax", "Max Other Motes")).build()
        ).addActionRow(TextInput.create("essence-amount", "Chose the amount to modify", TextInputStyle.SHORT).build()).queue();

                            @Override
    public void OnStringSelectInteraction(StringSelectInteractionEvent event) {
        if(event.getComponentId().equals("choose-essence")){

        }
    }

     */
}
